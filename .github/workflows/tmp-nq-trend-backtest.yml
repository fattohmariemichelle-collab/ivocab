name: Temporary NQ trend detector backtest

on:
  push:
    branches:
      - tmp-nq-trend-backtest-20260827
  workflow_dispatch:

permissions:
  contents: read

jobs:
  backtest:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: python -m pip install --disable-pip-version-check pandas numpy

      - name: Run trend-only backtest
        run: python tmp_backtests/nq_trend_detector_backtest.py | tee trend_backtest_stdout.txt

      - name: Upload detailed results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: nq-trend-backtest-results
          path: |
            trend_backtest_results/
            trend_backtest_stdout.txt
          if-no-files-found: warn
          retention-days: 7
