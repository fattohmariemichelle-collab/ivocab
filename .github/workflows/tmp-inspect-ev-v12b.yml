name: Inspect EV data endpoint after V12b preregistration
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  archive:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Endpoint inspection completed and archived.'
