name: Preregistered NQ V12 pattern discovery
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  archive:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'V12 original completed; archived for manual reproducibility only.'
